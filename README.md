# key-value-database

## Introduction

In-memory key-value database with with support of asynchronous replication (physic) and WAL (Write-Ahead Logging)

## Launch

```
make run-server
```

## Project configuration

You need to create `.env` file and fill all variables like in `.env.example`
